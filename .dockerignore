.venv
__pycache__
*.pyc
.pytest_cache
.git
.gitignore
# Never bake secrets or the local DB into the image — secrets go through
# `fly secrets`, and the DB lives on the mounted volume.
.env
*.db
resumes/
.cache/
handoff.md
README.md
deploy
# NOTE: scripts/ IS baked into the image on purpose — it holds operational tools
# meant to run in production (scripts.import_user / scripts.migrate_user via
# `fly ssh`). Don't re-add it here; the analysis-only scripts are tiny + harmless.
