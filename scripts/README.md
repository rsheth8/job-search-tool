# Local helper scripts (not imported by the app)

| Script | Purpose |
|---|---|
| `test_slack_upload.py` | Smoke-test Slack PDF upload (`--scopes-only` needs no user id) |

Run with the project venv: `.venv/bin/python3 scripts/<script>.py`
