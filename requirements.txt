fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pydantic==2.10.4
pydantic-settings==2.7.1
python-dotenv==1.0.1
apscheduler==3.11.0   # background reminder delivery loop (Phase 2)
# Optional real backends (stubs work without these installed)
anthropic>=0.105.0   # needs output_config structured outputs (Haiku 4.5)
twilio==9.4.1
httpx==0.28.1
pypdf>=5.1.0        # accurate page count for tectonic PDFs (flate-compressed)
# Push notifications (APNs). Only needed when PUSH_ENABLED=true — app/push.py
# imports them lazily and no-ops without them, so the suite runs without these.
pyjwt>=2.8.0        # the APNs bearer token
cryptography>=42.0  # ES256 signing of that token (PyJWT's backend)
h2>=4.1.0           # APNs is HTTP/2 only (httpx needs this for http2=True)
sentry-sdk>=2.0.0
# Dev/test
pytest==8.3.4
